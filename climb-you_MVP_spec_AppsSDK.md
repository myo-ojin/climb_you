# 「climb-you」MVP 仕様（Apps SDK 版）  
> 長期目標を日次クエストに分解し、**自然と登頂＝達成**へ導く“山登り”UX

---

## 結論（要約）
- **実装スタック**：**ChatGPT内アプリ（Apps SDK）**＋**自前バックエンドDB**の二層。耐久データ（目標・履歴・ストリーク・ランキング）は**外部DBに保存**。Widget Stateは**軽量なUI状態のみ**。  
- **Auth**：ChatGPTクライアントが**OAuth 2.1＋PKCE**を起動→MCPサーバ→自前API/DBへ。**最小権限・明示同意・多層防御**を遵守。  
- **将来対応**：**Sign in with ChatGPT**（公式の関心フォーム段階）に備え、Authをアダプタ化。

---

## 1. プロダクト概要
- **名称**：climb-you  
- **コンセプト**：目標＝山、日々の小クエスト＝登山行程。**毎日の達成→軌跡が地形に刻まれる**UIで内的動機を刺激。  
- **ワークフロー**：インストール → **オンボーディング（目標/マイルストン/実行意図）** → **日次クエスト生成** → **達成・ログ** → 週次**ミニ・ランキング**。  
- **稼働環境**：**ChatGPT内アプリ（Apps SDKプレビュー）**で即検証、外部アプリ（iOS/Android/Web）は後追い。

---

## 2. 主要価値（ユーザー視点）
- **“小さく確実”の積み上げ**：長期目標を3–5段のマイルストン→日次の**小タスク束（3本）**に分解。  
- **環境依存の実行意図**：If-Then（もしXならYする）を初回に作成し、**阻害要因**から再学習。  
- **比較は“同レベル帯のみ”**：健全性のため**グローバル順位は非表示**、週次の**同格コホート**で励ます。  
- **証跡主義**：完了定義・所要時間・証跡（スクショ/一言メモ）で**曖昧な達成**を排除。

---

## 3. 機能要件（MVP）
### 3.1 オンボーディング（5分）
- 長期目標（SMART）と期限・KPI  
- 自動マイルストン（3–5段）  
- **実行意図**（通勤/就寝前/週末などのIf-Then）  
※ 会話導線とUIを**Apps SDKのコンポーネント**で統合。

### 3.2 日次クエスト生成
- 入力：**可処分時間/エネルギー/環境/前日ログ**  
- 出力：**小/中/検証**の3本束（所要・完了定義・証跡）  
- 再生成・保留・次回繰越

### 3.3 達成・ログ
- 完了 / 見送り / 阻害（選択＋一言）  
- ストリーク更新・XP付与・日次サマリ

### 3.4 コミュニティ（最小）
- **同レベル帯ランキング**（週次XP/達成率）  
- **休息日制度**（週1までノーカウント）で連続断絶の負荷を緩和

---

## 4. 非機能要件
- **安全性**：**最小権限・明示同意・多層防御**（プロンプトインジェクション想定、入力検証、監査ログ）。  
- **プライバシ**：行動追跡・プロファイリングは**開示と同意が前提**。不要なメタデータ収集は禁止。  
- **耐久性**：アプリ会話はApps SDKが保持、**ユーザーデータは外部DB**に永続化。Widget Stateは**小容量（~4kトークン目安）**。  
- **互換性**：MCP準拠で**接続標準化**（将来の他クライアント採用も想定）。

---

## 5. アーキテクチャ（Apps SDK × MCP）
```
ChatGPT（会話）
   └─ iframe UI（Apps SDK：window.openai / Widget State）
        └─ callTool() → MCPサーバ（あなたのサーバ）
             └─ 自前API（Auth検証・業務ロジック）
                  └─ DB（Goals / Quests / Logs / Streaks / Leaderboards）
```

- **Apps SDK はプレビュー提供中**（年内に提出受付予定）。  
- **Storage指針**：耐久データは**自前バックエンド**に置く。  
- **Widget State**：セッション跨ぎの**軽量状態**（モデルに見える/4k目安）。  
- **Auth**：**Authorization Code＋PKCE**をChatGPTが起動→**同意スコープ**に基づきMCPツールを許可。

---

## 6. MCPツール定義（例）
- `goal.set`：長期目標のCRUD（SMART必須）  
- `milestone.plan`：自動分解（3–5段）  
- `quest.generate`：日次3本束生成  
- `quest.complete`：結果・阻害・証跡ログ  
- `leaderboard.fetch`：週次スナップショット取得  
> 各ツールは**JSON Schema**で入出力を宣言し、必要な**OAuthスコープ**を最小化（例：`quests:write`、`leaderboard:read`）。

---

## 7. データモデル（外部DB）
- `User`（匿名ID、同意TS）  
- `Goal`（タイトル/期限/KPI/基準）  
- `Milestone`（Goal外部キー、順序、目安期間）  
- `QuestTemplate`（Goal外部キー、所要、完了定義）  
- `Quest`（当日束・提示履歴）  
- `Log`（完了/見送り/阻害・一言・証跡）  
- `Streak`（連続日数、Freeze回数）  
- `LeaderboardSnapshot`（週次・同レベル帯）

**保持・削除方針（例）**  
- `Log`：**180日**ローテ  
- `Goal/Milestone`：ユーザー削除まで  
- **削除API**・**エクスポートAPI**を用意（APPI/ストア審査対策）

---

## 8. セキュリティ／プライバシ／コンプライアンス
- **Apps SDK指針**：最小権限・明示同意・破壊操作の再確認・監査ログ。  
- **データ慣行**：**監視/トラッキング/行動プロファイル**は**開示・同意・最小化**が前提。  
- **ストア申告**：  
  - Apple **App Review/Privacy Details**に収集カテゴリ・目的・リンク/追跡の有無を正確申告。  
  - Google **Data safety**フォームに用途別の収集・共有・必須性を記載。  
- **将来の外部配布**：規制変化に備え、**配布経路のリスク**も評価。

---

## 9. KPI（初期）
- **D1/D7/D30 リテンション**  
- **週次マイルストン到達率**  
- **阻害→再計画率**（インサイト価値）  
- **日次クエスト完了中央値**  
- **休息日利用率**（健全性）

---

## 10. リスクと対策
- **ランキングの副作用**（不健全競争/離脱）→ **同レベル帯限定**＋**休息制度**＋**匿名化**。  
- **仕様変動（Apps SDKプレビュー）**→ **Auth/LLM/Storageの抽象化**で差し替え可能に。  
- **プライバシ違反**→ **最小収集・明示同意・削除手続**をUIで一貫提示。

---

## 11. ロードマップ（2週間の初期実装）
- **W1**：オンボーディングUI（Apps SDK）／`goal.set`・`milestone.plan`・`quest.generate`ツール／外部DBスキーマ  
- **W2**：`quest.complete`＋ログ集計／**同レベル帯ランキング**のAPI・UI／**同意UI**・プライバシーポリシー草案／**E2Eテスト（会話回帰）**

---

## 12. 将来拡張
- **Sign in with ChatGPT**の正式提供時に**Authアダプタ**を差し替え（ユーザープランに応じた機能開放）  
- **外部アプリ**（iOS/Android/Web）への横展開：Apps SDKで検証した会話UX・データモデルを流用
