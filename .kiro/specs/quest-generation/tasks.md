# 実装タスク：日次クエスト生成機能

- [ ] 1. データベーススキーマとモデルの実装
  - [ ] 1.1 クエスト関連テーブルを作成する
    - Questsテーブルを作成する（type、difficulty、completion_criteriaなど）
    - QuestBundlesテーブルを作成する（generated_at、valid_until、adjustmentsなど）
    - QuestLogsテーブルを作成する（status、skip_reason、evidenceなど）
    - PostponedQuestsテーブルを作成する（valid_until、priorityなど）
    - _要件: 要件9_

  - [ ] 1.2 学習データテーブルを作成する
    - SuccessPatternsテーブルを作成する（success_rate、preferred_difficultyなど）
    - FailurePatternsテーブルを作成する（common_failure_reasons、problematic_quest_typesなど）
    - _要件: 要件7_

  - [ ] 1.3 データモデルとバリデーションを実装する
    - Questモデルクラスを作成する（type、difficulty、estimated_timeのバリデーション）
    - QuestBundleモデルクラスを作成する（3つのクエストの検証）
    - QuestLogモデルクラスを作成する
    - 入力バリデーション関数を実装する
    - _要件: 要件2_

- [ ] 2. 学習エンジンの実装
  - [ ] 2.1 パターン分析アルゴリズムを実装する
    - 過去30日間のクエストログ取得機能を実装する
    - 成功率計算ロジックを実装する（クエストタイプ別、難易度別、時間帯別）
    - 失敗理由の頻度集計機能を実装する
    - 成功パターンの特定ロジックを実装する
    - 失敗パターンの特定ロジックを実装する
    - _要件: 要件7.2, 要件7.3, 要件7.4_

  - [ ] 2.2 パターン保存と更新機能を実装する
    - SuccessPatternsテーブルへの保存処理を実装する
    - FailurePatternsテーブルへの保存処理を実装する
    - 定期的な更新ロジックを実装する（週次）
    - _要件: 要件7.11_

  - [ ] 2.3 パターン分析APIエンドポイントを実装する（POST /api/v1/patterns/analyze）
    - パターン分析ロジックを呼び出す処理を実装する
    - 分析結果のレスポンス生成を実装する
    - エラーハンドリングを追加する
    - _要件: 要件7_

- [ ] 3. クエスト生成エンジンの実装
  - [ ] 3.1 LLMプロンプトテンプレートを作成する
    - 基本プロンプトテンプレートを作成する（目標、進捗、プロファイル、パターンを含む）
    - 成功パターンを反映するプロンプト部分を実装する
    - 失敗パターンを回避するプロンプト部分を実装する
    - 前日ログを考慮するプロンプト部分を実装する
    - _要件: 要件2.1, 要件7_

  - [ ] 3.2 クエスト生成ロジックを実装する
    - 入力データの収集処理を実装する（Goal、Station、Profile、Patternsなど）
    - LLM呼び出し処理を実装する（OpenAI API / Anthropic API）
    - レスポンスのパース処理を実装する
    - 3つのクエスト（小・中・検証）の検証を実装する
    - 合計時間の調整ロジックを実装する
    - _要件: 要件2.1, 要件2.2, 要件2.6_

  - [ ] 3.3 難易度調整ロジックを実装する
    - Energy_Levelに基づく難易度調整を実装する
    - 前日ログに基づく難易度調整を実装する
    - 過去7日間の達成率に基づく調整を実装する
    - _要件: 要件2.7, 要件7.6, 要件7.7, 要件7.8, 要件7.9_

  - [ ] 3.4 フォールバック機能を実装する
    - LLMエラー時のテンプレートベース生成を実装する
    - デフォルトクエストセットを作成する
    - _要件: 要件2_

- [ ] 4. スケジューラーの実装
  - [ ] 4.1 スケジューラーのセットアップ
    - Cron / Cloud Schedulerの設定を作成する
    - タイムゾーン対応のスケジューリングロジックを実装する
    - アクティブユーザーリスト取得機能を実装する
    - _要件: 要件1.1_

  - [ ] 4.2 バッチ処理を実装する
    - ユーザーごとのクエスト生成処理を実装する
    - 並列処理（最大10並列）を実装する
    - バッチサイズ（100ユーザーずつ）の制御を実装する
    - _要件: 要件1.1_

  - [ ] 4.3 エラーハンドリングとリトライを実装する
    - 生成失敗時のリトライロジック（最大3回）を実装する
    - タイムアウト処理（60秒）を実装する
    - エラーログの記録を実装する
    - 管理者への通知機能を実装する
    - _要件: 要件1_

  - [ ] 4.4 生成結果の保存処理を実装する
    - QuestBundleのデータベース保存を実装する
    - 各Questのデータベース保存を実装する
    - 生成ログの記録を実装する
    - _要件: 要件1.2, 要件9.2_

- [ ] 5. バックエンドAPI エンドポイントの実装
  - [ ] 5.1 クエスト生成エンドポイントを実装する（POST /api/v1/quests/generate）
    - リクエストのバリデーションを実装する
    - クエスト生成エンジンを呼び出す処理を実装する
    - 調整内容（adjustments）の処理を実装する
    - レスポンス生成を実装する
    - エラーハンドリングを追加する
    - _要件: 要件1.8, 要件2_

  - [ ] 5.2 今日のクエスト取得エンドポイントを実装する（GET /api/v1/quests/today）
    - 当日生成されたQuestBundleの取得処理を実装する
    - クエストが未生成の場合の処理を実装する
    - レスポンス生成を実装する
    - _要件: 要件1.4_

  - [ ] 5.3 クエスト再生成エンドポイントを実装する（POST /api/v1/quests/regenerate）
    - 再生成理由の記録を実装する
    - 全体再生成ロジックを実装する
    - 個別クエスト再生成ロジックを実装する
    - 再生成回数制限（1日3回）を実装する
    - 理由に基づく調整ロジックを実装する
    - _要件: 要件4, 要件5_

  - [ ] 5.4 クエスト保留エンドポイントを実装する（POST /api/v1/quests/postpone）
    - 保留理由の記録を実装する
    - PostponedQuestsテーブルへの保存を実装する
    - 有効期限（7日間）の設定を実装する
    - 優先度の設定を実装する
    - _要件: 要件6_

  - [ ] 5.5 保留クエストの取得と統合を実装する
    - 保留クエストリストの取得処理を実装する
    - 次回クエスト生成時の優先提案ロジックを実装する
    - 有効期限切れクエストの自動削除を実装する
    - _要件: 要件6.5, 要件6.6, 要件6.7_

- [ ] 6. MCPツールの実装
  - [ ] 6.1 quest.generateツールを実装する
    - ツールのJSON Schemaを定義する（adjustmentsを含む）
    - バックエンドAPI（/api/v1/quests/generate）を呼び出す処理を実装する
    - OAuthスコープ `quests:write` を要求する
    - レスポンスの変換処理を実装する
    - _要件: 要件1, 要件2_

  - [ ] 6.2 quest.regenerateツールを実装する
    - ツールのJSON Schemaを定義する（questId、reasonを含む）
    - バックエンドAPI（/api/v1/quests/regenerate）を呼び出す処理を実装する
    - OAuthスコープ `quests:write` を要求する
    - _要件: 要件4, 要件5_

  - [ ] 6.3 quest.postponeツールを実装する
    - ツールのJSON Schemaを定義する（questId、reasonを含む）
    - バックエンドAPI（/api/v1/quests/postpone）を呼び出す処理を実装する
    - OAuthスコープ `quests:write` を要求する
    - _要件: 要件6_

  - [ ] 6.4 quest.getTodayツールを実装する
    - ツールのJSON Schemaを定義する
    - バックエンドAPI（/api/v1/quests/today）を呼び出す処理を実装する
    - OAuthスコープ `quests:read` を要求する
    - _要件: 要件1.4_

- [ ] 7. Apps SDK フロントエンドの実装
  - [ ] 7.1 今日のクエスト表示画面を実装する
    - quest.getTodayツールを呼び出す処理を実装する
    - 3つのクエストをカード形式で表示するUIを作成する
    - クエストの種類（小・中・検証）を色やアイコンで区別する
    - 各クエストの詳細表示機能を実装する
    - _要件: 要件3, 要件10.4, 要件10.5_

  - [ ] 7.2 「いつもと違う日」調整UIを実装する
    - 「いつもと違う日」ボタンを実装する
    - Available_Time調整UIを作成する
    - Energy_Level選択UIを作成する
    - Environment選択UIを作成する
    - quest.generateツール（adjustments付き）を呼び出す処理を実装する
    - _要件: 要件1.5, 要件1.6, 要件1.7, 要件1.8_

  - [ ] 7.3 クエスト再生成UIを実装する
    - 「再生成」ボタンを実装する
    - 再生成理由選択UIを作成する
    - 個別クエスト変更ボタンを実装する
    - quest.regenerateツールを呼び出す処理を実装する
    - 再生成回数制限の表示を実装する
    - _要件: 要件4, 要件5_

  - [ ] 7.4 クエスト保留UIを実装する
    - 各クエストに「保留」ボタンを実装する
    - 保留理由選択UIを作成する
    - quest.postponeツールを呼び出す処理を実装する
    - 保留確認メッセージを表示する
    - _要件: 要件6_

  - [ ] 7.5 クエスト承認と開始UIを実装する
    - 「今日のクエストを開始」ボタンを実装する
    - クエスト実行画面への遷移を実装する
    - チェックリスト形式の表示を実装する
    - _要件: 要件8_

  - [ ] 7.6 ローディングとメッセージUIを実装する
    - 生成中のローディング表示を実装する（「あなたに合ったクエストを生成中...」）
    - 完了時のモチベーションメッセージを実装する（「今日の山登りを始めましょう！」）
    - _要件: 要件10.3, 要件10.6_

- [ ] 8. エラーハンドリングとリカバリー機能の実装
  - [ ] 8.1 クライアント側エラーハンドリングを実装する
    - ネットワークエラー時のWidget State保存を実装する
    - 接続回復後の自動再送信を実装する
    - ユーザーフレンドリーなエラーメッセージを実装する
    - _要件: 要件9.5_

  - [ ] 8.2 サーバー側エラーハンドリングを実装する
    - 認証エラー（401）のハンドリングを実装する
    - 認可エラー（403）のハンドリングを実装する
    - バリデーションエラー（400）のハンドリングを実装する
    - サーバーエラー（500）のロギングとレスポンスを実装する
    - _要件: 要件9.4_

  - [ ] 8.3 スケジューラーエラーハンドリングを実装する
    - LLMタイムアウト時のフォールバック処理を実装する
    - 生成失敗時のリトライロジックを実装する
    - エラー通知機能を実装する
    - _要件: 要件1_

- [ ] 9. パフォーマンス最適化の実装
  - [ ] 9.1 LLM呼び出しの最適化
    - プロンプトのキャッシング機能を実装する
    - 類似パターンの再利用ロジックを実装する
    - レスポンスタイムの監視（5秒以内）を実装する
    - _要件: 要件2, 要件10.2_

  - [ ] 9.2 データベースの最適化
    - クエストログのパーティショニング（月ごと）を実装する
    - 必要なインデックスを作成する
    - 古いログの定期削除（180日後）を実装する
    - _要件: 要件7_

  - [ ] 9.3 スケジューラーの最適化
    - タイムゾーンごとのグループ化を実装する
    - バッチ処理の並列化を実装する
    - 処理時間の監視とログ記録を実装する
    - _要件: 要件1_

- [ ] 10. 統合とE2Eテストの実装
  - [ ] 10.1 自動生成フローのE2Eテストを作成する
    - スケジューラー起動からクエスト保存までのフローをテストする
    - タイムゾーン処理のテストを作成する
    - エラーケースのテストを作成する
    - _要件: 要件1_

  - [ ] 10.2 ユーザー調整フローのE2Eテストを作成する
    - 「いつもと違う日」調整からクエスト再生成までのフローをテストする
    - 再生成と保留のフローをテストする
    - _要件: 要件1, 要件4, 要件6_

  - [ ] 10.3 学習機能のテストを作成する
    - パターン分析アルゴリズムのテストを作成する
    - 学習データの反映テストを作成する
    - _要件: 要件7_

- [ ] 11. ドキュメントとデプロイ準備
  - [ ] 11.1 スケジューラー設定ドキュメントを作成する
    - Cron / Cloud Schedulerの設定手順を文書化する
    - タイムゾーン設定の説明を記載する
    - トラブルシューティングガイドを作成する
    - _要件: 要件1_

  - [ ] 11.2 API ドキュメントを作成する
    - 各エンドポイントの仕様を文書化する
    - MCPツールの使用方法を文書化する
    - _要件: 全要件_

  - [ ] 11.3 デプロイ設定を作成する
    - 環境変数の設定を文書化する
    - データベースマイグレーションスクリプトを作成する
    - スケジューラーのデプロイ手順を文書化する
    - _要件: 全要件_
