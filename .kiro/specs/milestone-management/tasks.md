# 実装タスク：マイルストーン達成管理機能

- [ ] 1. データベーススキーマとモデルの実装
  - [ ] 1.1 マイルストーン進捗テーブルを作成する
    - MilestoneProgressテーブルを作成する（user_id、station、achievement_status、progress_rateなど）
    - MilestoneAdjustmentsテーブルを作成する（調整履歴）
    - MilestoneRedesignsテーブルを作成する（再設計履歴）
    - StagnationAlertsテーブルを作成する（停滞検知データ）
    - _要件: 要件10_

  - [ ] 1.2 データモデルとバリデーションを実装する
    - MilestoneProgressモデルクラスを作成する
    - Achievement_Statusのバリデーションを実装する（未到達/到達済み/達成済み）
    - Progress_Rateのバリデーションを実装する（0〜100%）
    - _要件: 要件1, 要件2, 要件3_

- [ ] 2. 合目到達検知エンジンの実装
  - [ ] 2.1 到達判定ロジックを実装する
    - 累計歩数と必要歩数の比較処理を実装する
    - 合目到達イベントのトリガー処理を実装する
    - 到達通知の生成処理を実装する
    - _要件: 要件1.1, 要件1.2_

  - [ ] 2.2 到達確認UIトリガーを実装する
    - クエスト完了時の到達チェックを実装する
    - 到達確認画面の表示処理を実装する
    - Milestone_Criteriaの取得と表示を実装する
    - _要件: 要件1.3, 要件1.4_

- [ ] 3. 達成確認処理の実装
  - [ ] 3.1 「はい」選択時の処理を実装する
    - 証跡提出UIを実装する（画像/メモ/ファイル）
    - Achievement_Statusの更新処理を実装する
    - 合目達成のお祝いメッセージ生成を実装する
    - バッジ付与処理を実装する
    - 次の合目情報の取得と表示を実装する
    - _要件: 要件2_

  - [ ] 3.2 達成データの保存処理を実装する
    - 達成日時の記録を実装する
    - 証跡の保存処理を実装する
    - 全体進捗率の更新を実装する
    - _要件: 要件2.7, 要件2.8_

- [ ] 4. 未達成処理の実装
  - [ ] 4.1 「いいえ」選択時の処理を実装する
    - 未達成理由選択UI（4択＋自由記入）を実装する
    - 進捗率入力UI（スライダー/数値）を実装する
    - 3つの選択肢表示UIを実装する
    - _要件: 要件3_

  - [ ] 4.2 未達成データの保存処理を実装する
    - 未達成理由の記録を実装する
    - 進捗率の保存を実装する
    - 学習データとしての保存を実装する
    - _要件: 要件3.10_

- [ ] 5. 選択肢A「このまま続ける」の実装
  - [ ] 5.1 継続処理を実装する
    - 現在の合目維持処理を実装する
    - 励ましメッセージの生成を実装する
    - 残りタスクの明示処理を実装する
    - 優先フラグの設定を実装する
    - _要件: 要件4_

  - [ ] 5.2 次回クエスト生成への連携を実装する
    - 残りタスク優先生成フラグをデータベースに保存する
    - クエスト生成システムとの連携処理を実装する
    - _要件: 要件4.4_

- [ ] 6. 選択肢B「目標を調整する」の実装
  - [ ] 6.1 調整案生成ロジックを実装する
    - 進捗率に基づく調整案の計算を実装する
    - 調整理由の説明文生成を実装する
    - 調整案の表示UIを実装する
    - _要件: 要件5.2, 要件5.3_

  - [ ] 6.2 調整承認処理を実装する
    - Milestone_Criteriaの更新処理を実装する
    - 即座の合目達成処理を実装する
    - 達成メッセージの表示を実装する
    - 次の合目への遷移処理を実装する
    - _要件: 要件5.5, 要件5.6, 要件5.7, 要件5.8_

  - [ ] 6.3 カスタム調整処理を実装する
    - 調整案拒否時のカスタム入力UIを実装する
    - カスタム調整の検証処理を実装する
    - カスタム調整の保存処理を実装する
    - _要件: 要件5.10_

  - [ ] 6.4 調整履歴の記録を実装する
    - 調整前後の比較データの保存を実装する
    - 調整理由の記録を実装する
    - _要件: 要件5.9, 要件10.7_

- [ ] 7. 選択肢C「マイルストーンを再設計する」の実装
  - [ ] 7.1 再設計理由の確認処理を実装する
    - 再設計理由の入力UIを実装する
    - 再設計オプションの表示UIを実装する（細分化/難易度低下/期間延長）
    - _要件: 要件6.1, 要件6.2, 要件6.3_

  - [ ] 7.2 マイルストーン再生成処理を実装する
    - LLMを使用した再生成ロジックを実装する
    - 現在の合目以降の再生成処理を実装する
    - 再生成されたマイルストーンの表示UIを実装する
    - 変更前後の比較表示を実装する
    - _要件: 要件6.4, 要件6.5, 要件6.6_

  - [ ] 7.3 再設計承認処理を実装する
    - データベースの更新処理を実装する
    - 歩数と進捗の引き継ぎ処理を実装する
    - 再設計完了メッセージの表示を実装する
    - _要件: 要件6.7, 要件6.8, 要件6.9_

  - [ ] 7.4 再設計履歴の記録を実装する
    - 再設計前後のマイルストーンの保存を実装する
    - 再設計理由の記録を実装する
    - _要件: 要件6.10, 要件10.7_

- [ ] 8. 停滞検知とサポートの実装
  - [ ] 8.1 停滞検知ロジックを実装する
    - 合目滞在日数の記録処理を実装する
    - 30日以上滞在の検知処理を実装する
    - 停滞アラートの生成処理を実装する
    - _要件: 要件7.1, 要件7.2_

  - [ ] 8.2 停滞原因の探索処理を実装する
    - 原因質問UIを実装する
    - 原因選択肢（4択＋自由記入）を実装する
    - 原因データの保存処理を実装する
    - _要件: 要件7.3, 要件7.4_

  - [ ] 8.3 停滞時の提案処理を実装する
    - 3つの提案の生成ロジックを実装する
    - 提案表示UIを実装する
    - 提案選択時の処理分岐を実装する
    - _要件: 要件7.5, 要件7.6, 要件7.7, 要件7.8, 要件7.9_

  - [ ] 8.4 停滞データの分析処理を実装する
    - 停滞パターンの分析ロジックを実装する
    - 将来のマイルストーン生成への反映処理を実装する
    - _要件: 要件7.10_

- [ ] 9. 複数回未達成への対応の実装
  - [ ] 9.1 未達成回数の記録処理を実装する
    - 同じ合目での未達成カウント処理を実装する
    - 3回連続未達成の検知処理を実装する
    - 特別サポート画面のトリガー処理を実装する
    - _要件: 要件8.1, 要件8.2_

  - [ ] 9.2 振り返りセッションの実装
    - 振り返り質問UIを実装する
    - 質問の順次表示処理を実装する
    - 回答の記録処理を実装する
    - _要件: 要件8.3, 要件8.4, 要件8.5, 要件8.6_

  - [ ] 9.3 分析と提案処理の実装
    - 回答の分析ロジックを実装する
    - 最適な対応の提案生成を実装する
    - 提案実行のサポート処理を実装する
    - _要件: 要件8.7, 要件8.8_

  - [ ] 9.4 振り返りデータの活用処理を実装する
    - 振り返り内容の記録を実装する
    - 次回クエスト生成への反映処理を実装する
    - _要件: 要件8.9, 要件8.10_

- [ ] 10. バックエンドAPI エンドポイントの実装
  - [ ] 10.1 合目到達確認エンドポイントを実装する（POST /api/v1/milestones/:station/check）
    - 到達判定処理を実装する
    - 確認質問データの生成を実装する
    - レスポンス生成を実装する
    - _要件: 要件1_

  - [ ] 10.2 合目達成エンドポイントを実装する（POST /api/v1/milestones/:station/achieve）
    - 証跡の保存処理を実装する
    - Achievement_Statusの更新を実装する
    - バッジ付与処理を実装する
    - 次の合目情報の取得を実装する
    - _要件: 要件2_

  - [ ] 10.3 未達成記録エンドポイントを実装する（POST /api/v1/milestones/:station/not-achieved）
    - 未達成理由の保存を実装する
    - 進捗率の記録を実装する
    - 学習データとしての保存を実装する
    - _要件: 要件3_

  - [ ] 10.4 継続選択エンドポイントを実装する（POST /api/v1/milestones/:station/continue）
    - 優先フラグの設定を実装する
    - 励ましメッセージの生成を実装する
    - _要件: 要件4_

  - [ ] 10.5 目標調整エンドポイントを実装する（POST /api/v1/milestones/:station/adjust）
    - 調整案の生成を実装する
    - 調整承認処理を実装する
    - Milestone_Criteriaの更新を実装する
    - _要件: 要件5_

  - [ ] 10.6 マイルストーン再設計エンドポイントを実装する（POST /api/v1/milestones/redesign）
    - LLMを使用した再生成処理を実装する
    - 再設計承認処理を実装する
    - データベースの更新を実装する
    - _要件: 要件6_

  - [ ] 10.7 停滞検知エンドポイントを実装する（GET /api/v1/milestones/stagnation）
    - 滞在日数の計算を実装する
    - 停滞判定処理を実装する
    - アラート生成を実装する
    - _要件: 要件7_

  - [ ] 10.8 進捗取得エンドポイントを実装する（GET /api/v1/milestones/progress）
    - 全合目の進捗データ取得を実装する
    - 現在地の特定を実装する
    - 次の合目までの情報計算を実装する
    - _要件: 要件9_

- [ ] 11. MCPツールの実装
  - [ ] 11.1 milestone.checkAchievementツールを実装する
    - ツールのJSON Schemaを定義する
    - バックエンドAPI（/api/v1/milestones/:station/check）を呼び出す処理を実装する
    - OAuthスコープ `milestones:read` を要求する
    - _要件: 要件1_

  - [ ] 11.2 milestone.achieveツールを実装する
    - ツールのJSON Schemaを定義する（evidenceを含む）
    - バックエンドAPI（/api/v1/milestones/:station/achieve）を呼び出す処理を実装する
    - OAuthスコープ `milestones:write` を要求する
    - _要件: 要件2_

  - [ ] 11.3 milestone.recordNotAchievedツールを実装する
    - ツールのJSON Schemaを定義する（reason、progress_rateを含む）
    - バックエンドAPI（/api/v1/milestones/:station/not-achieved）を呼び出す処理を実装する
    - OAuthスコープ `milestones:write` を要求する
    - _要件: 要件3_

  - [ ] 11.4 milestone.adjustツールを実装する
    - ツールのJSON Schemaを定義する
    - バックエンドAPI（/api/v1/milestones/:station/adjust）を呼び出す処理を実装する
    - OAuthスコープ `milestones:write` を要求する
    - _要件: 要件5_

  - [ ] 11.5 milestone.redesignツールを実装する
    - ツールのJSON Schemaを定義する（optionsを含む）
    - バックエンドAPI（/api/v1/milestones/redesign）を呼び出す処理を実装する
    - OAuthスコープ `milestones:write` を要求する
    - _要件: 要件6_

  - [ ] 11.6 milestone.getProgressツールを実装する
    - ツールのJSON Schemaを定義する
    - バックエンドAPI（/api/v1/milestones/progress）を呼び出す処理を実装する
    - OAuthスコープ `milestones:read` を要求する
    - _要件: 要件9_

- [ ] 12. Apps SDK フロントエンドの実装
  - [ ] 12.1 合目到達確認UIを実装する
    - 到達通知の表示を実装する
    - Milestone_Criteriaの表示を実装する
    - 「はい」「いいえ」選択UIを実装する
    - milestone.checkAchievementツールを呼び出す処理を実装する
    - _要件: 要件1_

  - [ ] 12.2 達成確認UIを実装する
    - 証跡提出UI（画像/メモ/ファイル）を実装する
    - お祝いメッセージとアニメーションを実装する
    - バッジ表示を実装する
    - 次の合目情報表示を実装する
    - milestone.achieveツールを呼び出す処理を実装する
    - _要件: 要件2, 要件11.2_

  - [ ] 12.3 未達成確認UIを実装する
    - 未達成理由選択UI（4択＋自由記入）を実装する
    - 進捗率入力UI（スライダー）を実装する
    - 3つの選択肢表示UIを実装する
    - milestone.recordNotAchievedツールを呼び出す処理を実装する
    - _要件: 要件3_

  - [ ] 12.4 継続選択UIを実装する
    - 励ましメッセージの表示を実装する
    - 残りタスクの表示を実装する
    - 継続確認ボタンを実装する
    - _要件: 要件4, 要件11.3_

  - [ ] 12.5 目標調整UIを実装する
    - 調整案の表示を実装する
    - 調整理由の説明表示を実装する
    - 承認/拒否ボタンを実装する
    - カスタム調整入力UIを実装する
    - milestone.adjustツールを呼び出す処理を実装する
    - _要件: 要件5_

  - [ ] 12.6 マイルストーン再設計UIを実装する
    - 再設計理由入力UIを実装する
    - 再設計オプション選択UIを実装する
    - 変更前後の比較表示を実装する
    - 承認ボタンを実装する
    - milestone.redesignツールを呼び出す処理を実装する
    - _要件: 要件6_

  - [ ] 12.7 停滞アラートUIを実装する
    - 停滞アラートの表示を実装する
    - 原因探索質問UIを実装する
    - 3つの提案表示UIを実装する
    - _要件: 要件7, 要件11.4_

  - [ ] 12.8 振り返りセッションUIを実装する
    - 特別サポート画面を実装する
    - 振り返り質問の順次表示を実装する
    - 回答入力UIを実装する
    - 分析結果と提案の表示を実装する
    - _要件: 要件8_

  - [ ] 12.9 進捗可視化UIを実装する
    - 10合目の山のビジュアルを実装する
    - 達成済み合目の特別表示を実装する
    - 現在地の強調表示を実装する
    - 進捗率の表示を実装する
    - 推定到達日の表示を実装する
    - 合目詳細表示（タップ時）を実装する
    - milestone.getProgressツールを呼び出す処理を実装する
    - _要件: 要件9_

- [ ] 13. エラーハンドリングとリカバリー機能の実装
  - [ ] 13.1 クライアント側エラーハンドリングを実装する
    - ネットワークエラー時のWidget State保存を実装する
    - 接続回復後の自動再送信を実装する
    - ユーザーフレンドリーなエラーメッセージを実装する
    - _要件: 要件10.5_

  - [ ] 13.2 サーバー側エラーハンドリングを実装する
    - 認証エラー（401）のハンドリングを実装する
    - 認可エラー（403）のハンドリングを実装する
    - バリデーションエラー（400）のハンドリングを実装する
    - サーバーエラー（500）のロギングとレスポンスを実装する
    - _要件: 要件10.4_

  - [ ] 13.3 LLM再生成のエラーハンドリングを実装する
    - LLMタイムアウト時のフォールバック処理を実装する
    - 再生成失敗時のリトライ処理を実装する
    - エラー通知機能を実装する
    - _要件: 要件6_

- [ ] 14. パフォーマンス最適化の実装
  - [ ] 14.1 データベースの最適化
    - MilestoneProgressテーブルのインデックスを作成する
    - 滞在日数計算のクエリ最適化を実装する
    - 進捗データのキャッシング処理を実装する
    - _要件: 要件7, 要件9_

  - [ ] 14.2 UIのパフォーマンス最適化
    - 確認プロセスの最適化（90秒以内）を実装する
    - アニメーションのパフォーマンス最適化を実装する
    - 山のビジュアルのレンダリング最適化を実装する
    - _要件: 要件11.6_

- [ ] 15. 統合とE2Eテストの実装
  - [ ] 15.1 合目達成フローのE2Eテストを作成する
    - 到達検知から達成確認までのフローをテストする
    - 証跡提出のテストを作成する
    - バッジ付与のテストを作成する
    - _要件: 要件1, 要件2_

  - [ ] 15.2 未達成フローのE2Eテストを作成する
    - 未達成記録のフローをテストする
    - 3つの選択肢のフローをテストする
    - 調整と再設計のテストを作成する
    - _要件: 要件3, 要件4, 要件5, 要件6_

  - [ ] 15.3 停滞検知フローのE2Eテストを作成する
    - 停滞検知のテストを作成する
    - 原因探索のテストを作成する
    - 提案処理のテストを作成する
    - _要件: 要件7_

  - [ ] 15.4 複数回未達成フローのE2Eテストを作成する
    - 未達成カウントのテストを作成する
    - 振り返りセッションのテストを作成する
    - 分析と提案のテストを作成する
    - _要件: 要件8_

- [ ] 16. ドキュメントとデプロイ準備
  - [ ] 16.1 API ドキュメントを作成する
    - 各エンドポイントの仕様を文書化する
    - MCPツールの使用方法を文書化する
    - _要件: 全要件_

  - [ ] 16.2 ユーザーガイドを作成する
    - 合目達成の仕組みを文書化する
    - 未達成時の対応方法を文書化する
    - 停滞時のサポート機能を文書化する
    - _要件: 全要件_

  - [ ] 16.3 デプロイ設定を作成する
    - 環境変数の設定を文書化する
    - データベースマイグレーションスクリプトを作成する
    - _要件: 全要件_
