# 実装タスク：クエスト達成ログ機能

- [ ] 1. データベーススキーマとモデルの実装
  - [ ] 1.1 ログ関連テーブルを作成する
    - QuestLogsテーブルを作成する（quest_id、status、actual_time、evidenceなど）
    - Streaksテーブルを作成する（current_streak、max_streak、last_updated）
    - FreezeDaysテーブルを作成する（week_start、used_count）
    - DailySummariesテーブルを作成する（date、completion_rate、total_stepsなど）
    - ClimbingProgressテーブルを作成する（total_steps、current_station、station_progressなど）
    - _要件: 要件9_

  - [ ] 1.2 データモデルとバリデーションを実装する
    - QuestLogモデルクラスを作成する（status、evidence_typeのバリデーション）
    - Streakモデルクラスを作成する
    - DailySummaryモデルクラスを作成する
    - 入力バリデーション関数を実装する
    - _要件: 要件1, 要件2, 要件3_

- [ ] 2. 歩数計算エンジンの実装
  - [ ] 2.1 歩数付与ロジックを実装する
    - 難易度別の歩数計算関数を実装する（easy: 50歩、medium: 100歩、challenging: 150歩）
    - クエストタイプ別の歩数計算を実装する（validation: 30歩）
    - コンプリートボーナス計算を実装する（+50歩）
    - _要件: 要件6.2, 要件6.3, 要件6.4, 要件6.5, 要件6.6_

  - [ ] 2.2 累計歩数管理を実装する
    - 累計歩数の更新処理を実装する
    - 合目ごとの進捗率計算を実装する
    - 次の合目までの必要歩数計算を実装する
    - _要件: 要件6.7, 要件6.9, 要件8.1, 要件8.2_

  - [ ] 2.3 歩数表示ロジックを実装する
    - 山登りメタファーのメッセージ生成を実装する（「+100歩登りました！」）
    - 進捗表示の文言生成を実装する（「3合目まであと250歩」）
    - _要件: 要件6.8, 要件6.9_

- [ ] 3. バックエンドAPI エンドポイントの実装
  - [ ] 3.1 クエスト完了エンドポイントを実装する（POST /api/v1/quests/:id/complete）
    - リクエストのバリデーションを実装する（actual_time、evidence）
    - クエストステータスの更新処理を実装する
    - 歩数付与処理を実装する
    - ストリーク更新処理を実装する
    - レスポンス生成を実装する（付与された歩数、更新されたストリークを含む）
    - _要件: 要件1, 要件6_

  - [ ] 3.2 クエスト見送りエンドポイントを実装する（POST /api/v1/quests/:id/skip）
    - 見送り理由のバリデーションを実装する
    - クエストステータスの更新処理を実装する
    - 学習データとしての保存処理を実装する
    - ストリーク判定処理を実装する（部分達成の許容）
    - _要件: 要件2, 要件4.2_

  - [ ] 3.3 クエスト阻害エンドポイントを実装する（POST /api/v1/quests/:id/obstruct）
    - 阻害要因のバリデーションを実装する
    - クエストステータスの更新処理を実装する
    - WOOP障害データとしての保存処理を実装する
    - 対処計画の記録処理を実装する
    - _要件: 要件3_

  - [ ] 3.4 日次サマリ生成エンドポイントを実装する（POST /api/v1/summaries/daily）
    - 1日のクエスト集計処理を実装する
    - 達成率計算を実装する
    - 獲得歩数の集計を実装する
    - サマリデータの保存処理を実装する
    - _要件: 要件7_

  - [ ] 3.5 ストリーク管理エンドポイントを実装する（GET /api/v1/streaks、PUT /api/v1/streaks）
    - 現在のストリーク取得処理を実装する
    - ストリーク更新ロジックを実装する
    - 最高記録の更新処理を実装する
    - マイルストン達成判定を実装する（7日、30日、100日）
    - _要件: 要件4_

  - [ ] 3.6 休息日管理エンドポイントを実装する（POST /api/v1/freeze-days/use、GET /api/v1/freeze-days/status）
    - 休息日使用処理を実装する
    - 週ごとのリセット処理を実装する
    - 残り回数の取得処理を実装する
    - 使用制限チェックを実装する（週1回まで）
    - _要件: 要件5_

  - [ ] 3.7 履歴取得エンドポイントを実装する（GET /api/v1/quests/logs）
    - ログの日付順取得処理を実装する
    - フィルタリング機能を実装する（status別）
    - 検索機能を実装する（キーワード検索）
    - ページネーション処理を実装する
    - _要件: 要件10_

  - [ ] 3.8 統計取得エンドポイントを実装する（GET /api/v1/statistics）
    - 週次統計の計算を実装する（達成率、平均所要時間、獲得歩数）
    - 月次統計の計算を実装する
    - 統計データのキャッシング処理を実装する
    - _要件: 要件10.5_

- [ ] 4. MCPツールの実装
  - [ ] 4.1 quest.completeツールを実装する
    - ツールのJSON Schemaを定義する（actual_time、evidenceを含む）
    - バックエンドAPI（/api/v1/quests/:id/complete）を呼び出す処理を実装する
    - OAuthスコープ `quests:write` を要求する
    - レスポンスの変換処理を実装する
    - _要件: 要件1_

  - [ ] 4.2 quest.skipツールを実装する
    - ツールのJSON Schemaを定義する（reason、noteを含む）
    - バックエンドAPI（/api/v1/quests/:id/skip）を呼び出す処理を実装する
    - OAuthスコープ `quests:write` を要求する
    - _要件: 要件2_

  - [ ] 4.3 quest.obstructツールを実装する
    - ツールのJSON Schemaを定義する（obstacle、planを含む）
    - バックエンドAPI（/api/v1/quests/:id/obstruct）を呼び出す処理を実装する
    - OAuthスコープ `quests:write` を要求する
    - _要件: 要件3_

  - [ ] 4.4 summary.getDailyツールを実装する
    - ツールのJSON Schemaを定義する
    - バックエンドAPI（/api/v1/summaries/daily）を呼び出す処理を実装する
    - OAuthスコープ `quests:read` を要求する
    - _要件: 要件7_

  - [ ] 4.5 freezeDay.useツールを実装する
    - ツールのJSON Schemaを定義する
    - バックエンドAPI（/api/v1/freeze-days/use）を呼び出す処理を実装する
    - OAuthスコープ `quests:write` を要求する
    - _要件: 要件5_

  - [ ] 4.6 logs.getHistoryツールを実装する
    - ツールのJSON Schemaを定義する（filter、searchを含む）
    - バックエンドAPI（/api/v1/quests/logs）を呼び出す処理を実装する
    - OAuthスコープ `quests:read` を要求する
    - _要件: 要件10_

- [ ] 5. Apps SDK フロントエンドの実装
  - [ ] 5.1 クエスト完了UIを実装する
    - 完了確認画面を作成する
    - Actual_Time入力UI（4択＋自由記入）を実装する
    - Evidence入力UI（画像/メモ/ファイル）を実装する
    - 一言メモ入力フィールドを実装する
    - quest.completeツールを呼び出す処理を実装する
    - _要件: 要件1_

  - [ ] 5.2 クエスト見送りUIを実装する
    - 見送り理由選択UI（4択＋自由記入）を実装する
    - 詳細メモ入力フィールドを実装する
    - quest.skipツールを呼び出す処理を実装する
    - 励ましのメッセージを表示する
    - _要件: 要件2, 要件11.3_

  - [ ] 5.3 クエスト阻害UIを実装する
    - 阻害要因選択UI（4択＋自由記入）を実装する
    - 詳細説明入力フィールドを実装する
    - 対処計画入力フィールドを実装する
    - quest.obstructツールを呼び出す処理を実装する
    - サポートメッセージを表示する
    - _要件: 要件3, 要件11.4_

  - [ ] 5.4 歩数表示UIを実装する
    - 獲得歩数のアニメーション表示を実装する（「+100歩登りました！」）
    - 累計歩数の表示を実装する
    - 次の合目までの進捗表示を実装する
    - 山のビジュアルに歩数を反映する
    - _要件: 要件6.8, 要件6.9, 要件8_

  - [ ] 5.5 ストリーク表示UIを実装する
    - 現在のストリーク表示を実装する
    - ストリーク更新時のアニメーションを実装する
    - マイルストン達成時の特別メッセージを実装する（7日、30日、100日）
    - 最高記録の表示を実装する
    - _要件: 要件4_

  - [ ] 5.6 休息日UIを実装する
    - 休息日使用ボタンを実装する
    - 残り回数の表示を実装する
    - 使用確認ダイアログを実装する
    - freezeDay.useツールを呼び出す処理を実装する
    - _要件: 要件5_

  - [ ] 5.7 日次サマリUIを実装する
    - サマリ画面のレイアウトを作成する
    - 達成率の視覚化（グラフ/アイコン）を実装する
    - 獲得歩数の表示を実装する
    - ストリークの表示を実装する
    - 達成率に応じたメッセージ表示を実装する（100%/0%の特別メッセージ）
    - summary.getDailyツールを呼び出す処理を実装する
    - _要件: 要件7, 要件11.2_

  - [ ] 5.8 履歴閲覧UIを実装する
    - ログリスト表示を実装する
    - フィルタリングUI（完了/見送り/阻害）を実装する
    - 検索UIを実装する
    - 証跡表示機能を実装する
    - 週次・月次統計表示を実装する
    - logs.getHistoryツールを呼び出す処理を実装する
    - _要件: 要件10_

  - [ ] 5.9 進捗可視化UIを実装する
    - 山のビジュアルコンポーネントを作成する
    - 10合目の表示を実装する
    - 現在地の強調表示を実装する
    - 合目ごとの進捗率表示を実装する
    - 全体進捗率の表示を実装する
    - _要件: 要件8_

- [ ] 6. エラーハンドリングとリカバリー機能の実装
  - [ ] 6.1 クライアント側エラーハンドリングを実装する
    - ネットワークエラー時のWidget State保存を実装する
    - 接続回復後の自動再送信を実装する
    - ユーザーフレンドリーなエラーメッセージを実装する
    - _要件: 要件9.5_

  - [ ] 6.2 サーバー側エラーハンドリングを実装する
    - 認証エラー（401）のハンドリングを実装する
    - 認可エラー（403）のハンドリングを実装する
    - バリデーションエラー（400）のハンドリングを実装する
    - サーバーエラー（500）のロギングとレスポンスを実装する
    - _要件: 要件9.4_

  - [ ] 6.3 証跡アップロードのエラーハンドリングを実装する
    - ファイルサイズ制限のチェックを実装する
    - ファイル形式のバリデーションを実装する
    - アップロード失敗時のリトライ処理を実装する
    - _要件: 要件1_

- [ ] 7. パフォーマンス最適化の実装
  - [ ] 7.1 データベースの最適化
    - QuestLogsテーブルのインデックスを作成する（user_id、quest_id、created_at）
    - 古いログの定期削除処理を実装する（180日後）
    - 統計データのキャッシング処理を実装する
    - _要件: 要件9.6, 要件9.7_

  - [ ] 7.2 証跡ストレージの最適化
    - 画像の圧縮処理を実装する
    - CDNへのアップロード処理を実装する
    - サムネイル生成処理を実装する
    - _要件: 要件1_

  - [ ] 7.3 UIのパフォーマンス最適化
    - ログ記録プロセスの最適化（60秒以内）を実装する
    - アニメーションのパフォーマンス最適化を実装する
    - 履歴表示の遅延ロードを実装する
    - _要件: 要件1.12, 要件11.5_

- [ ] 8. 統合とE2Eテストの実装
  - [ ] 8.1 クエスト完了フローのE2Eテストを作成する
    - 完了記録から歩数付与までのフローをテストする
    - ストリーク更新のテストを作成する
    - 証跡アップロードのテストを作成する
    - _要件: 要件1, 要件4, 要件6_

  - [ ] 8.2 見送り・阻害フローのE2Eテストを作成する
    - 見送り記録のフローをテストする
    - 阻害記録のフローをテストする
    - 学習データ保存のテストを作成する
    - _要件: 要件2, 要件3_

  - [ ] 8.3 日次サマリ生成のE2Eテストを作成する
    - サマリ生成のフローをテストする
    - 達成率計算のテストを作成する
    - メッセージ表示のテストを作成する
    - _要件: 要件7_

  - [ ] 8.4 休息日機能のE2Eテストを作成する
    - 休息日使用のフローをテストする
    - 週ごとのリセットのテストを作成する
    - 使用制限のテストを作成する
    - _要件: 要件5_

- [ ] 9. ドキュメントとデプロイ準備
  - [ ] 9.1 API ドキュメントを作成する
    - 各エンドポイントの仕様を文書化する
    - MCPツールの使用方法を文書化する
    - 証跡アップロードの仕様を文書化する
    - _要件: 全要件_

  - [ ] 9.2 ユーザーガイドを作成する
    - クエスト完了の手順を文書化する
    - 歩数システムの説明を作成する
    - 休息日の使い方を文書化する
    - _要件: 全要件_

  - [ ] 9.3 デプロイ設定を作成する
    - 環境変数の設定を文書化する
    - データベースマイグレーションスクリプトを作成する
    - 証跡ストレージの設定を文書化する
    - _要件: 全要件_
