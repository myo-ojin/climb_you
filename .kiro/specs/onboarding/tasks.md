# 実装タスク：オンボーディング機能

- [ ] 1. プロジェクト構造とコア インターフェースのセットアップ
  - プロジェクトのディレクトリ構造を作成する（apps-sdk、mcp-server、backend-api、database）
  - TypeScript設定ファイルを作成する
  - 依存関係をインストールする（Apps SDK、MCP SDK、Express/Fastify、データベースクライアント）
  - コアインターフェースとタイプ定義を作成する（Goal、Milestone、UserProfile、OnboardingState）
  - _要件: 要件1, 要件2, 要件3, 要件4_

- [ ] 2. データベーススキーマとモデルの実装
  - [ ] 2.1 データベース接続ユーティリティを実装する
    - データベース接続プールを作成する
    - 接続エラーハンドリングを実装する
    - マイグレーションツールをセットアップする
    - _要件: 要件5.1_

  - [ ] 2.2 データベーステーブルを作成する
    - Usersテーブルを作成する（id、anonymous_id、consent_timestamp）
    - Goalsテーブルを作成する（SMART + WOOP フィールドを含む）
    - UserProfilesテーブルを作成する（7つのプロファイル項目）
    - Milestonesテーブルを作成する（10合目システム）
    - _要件: 要件5.2_

  - [ ] 2.3 データモデルとバリデーションを実装する
    - Goalモデルクラスを作成する（SMART + WOOP バリデーション）
    - UserProfileモデルクラスを作成する
    - Milestoneモデルクラスを作成する（1-10の範囲チェック）
    - 入力バリデーション関数を実装する
    - _要件: 要件1, 要件3, 要件4_

- [ ] 3. バックエンドAPI エンドポイントの実装
  - [ ] 3.1 認証ミドルウェアを実装する
    - OAuth 2.1 + PKCE 検証ミドルウェアを作成する
    - トークン検証ロジックを実装する
    - スコープチェック機能を実装する
    - _要件: 要件5.1, 要件6_

  - [ ] 3.2 目標分析エンドポイントを実装する（POST /api/v1/goals/analyze）
    - SMART基準分析ロジックを実装する（LLMを使用）
    - 不足要素の特定機能を実装する
    - 改善提案生成機能を実装する
    - エラーハンドリングを追加する
    - _要件: 要件1.2, 要件1.3_

  - [ ] 3.3 目標作成エンドポイントを実装する（POST /api/v1/goals）
    - 目標作成ロジックを実装する（SMART + WOOP）
    - データベースへの保存処理を実装する
    - レスポンス生成を実装する
    - エラーハンドリングを追加する
    - _要件: 要件1.7, 要件5.2_

  - [ ] 3.4 プロファイル作成エンドポイントを実装する（POST /api/v1/profiles）
    - プロファイル作成ロジックを実装する
    - 7つの質問項目のバリデーションを実装する
    - データベースへの保存処理を実装する
    - _要件: 要件3.13, 要件5.2_

  - [ ] 3.5 マイルストン生成エンドポイントを実装する（POST /api/v1/milestones/generate）
    - 10合目マイルストン生成ロジックを実装する（LLMを使用）
    - 目標、期間、コミットタイム、プロファイルを考慮したアルゴリズムを実装する
    - 各合目の到達目標、期間、達成条件を生成する
    - データベースへの保存処理を実装する
    - _要件: 要件4.1, 要件4.2, 要件4.3_

- [ ] 4. MCPサーバーとツール定義の実装
  - [ ] 4.1 MCPサーバーのセットアップ
    - MCPサーバーの基本構造を作成する
    - ツール登録機能を実装する
    - バックエンドAPIとの通信層を実装する
    - _要件: 要件5.1_

  - [ ] 4.2 goal.analyzeツールを実装する
    - ツールのJSON Schemaを定義する
    - バックエンドAPI（/api/v1/goals/analyze）を呼び出す処理を実装する
    - レスポンスの変換処理を実装する
    - _要件: 要件1.2_

  - [ ] 4.3 goal.createツールを実装する
    - ツールのJSON Schemaを定義する（SMART + WOOP フィールド）
    - バックエンドAPI（/api/v1/goals）を呼び出す処理を実装する
    - OAuthスコープ `goals:write` を要求する
    - _要件: 要件1.7, 要件5.1_

  - [ ] 4.4 profile.createツールを実装する
    - ツールのJSON Schemaを定義する（7つのプロファイル項目）
    - バックエンドAPI（/api/v1/profiles）を呼び出す処理を実装する
    - OAuthスコープ `profile:write` を要求する
    - _要件: 要件3.13_

  - [ ] 4.5 milestone.generateツールを実装する
    - ツールのJSON Schemaを定義する
    - バックエンドAPI（/api/v1/milestones/generate）を呼び出す処理を実装する
    - OAuthスコープ `goals:read`, `milestones:write` を要求する
    - _要件: 要件4.1_

- [ ] 5. Apps SDK フロントエンドの実装
  - [ ] 5.1 OnboardingFlowコンポーネントを作成する
    - 基本的なコンポーネント構造を作成する
    - Widget State管理を実装する
    - ステップ遷移ロジックを実装する
    - 進捗表示UIを実装する
    - _要件: 要件7.2, 要件7.4_

  - [ ] 5.2 プライバシー同意画面を実装する
    - 同意画面UIを作成する
    - データ収集内容の説明を表示する
    - 同意/拒否の処理を実装する
    - 同意タイムスタンプの記録を実装する
    - _要件: 要件6.1, 要件6.2, 要件6.3, 要件6.4_

  - [ ] 5.3 目標入力とSMART分析UIを実装する
    - 目標入力フォームを作成する
    - goal.analyzeツールを呼び出す処理を実装する
    - SMART分析結果の表示UIを実装する
    - 不足要素の改善質問UIを実装する
    - _要件: 要件1.1, 要件1.2, 要件1.3_

  - [ ] 5.4 WOOP質問UIを実装する
    - 期待する結果（Outcome）の入力UIを作成する
    - 予想される障害（Obstacle）の入力UIを作成する
    - 対処計画（Plan）の入力UIを作成する
    - _要件: 要件1.4, 要件1.5, 要件1.6_

  - [ ] 5.5 期間選択UIを実装する
    - 4択＋自由記入の選択UIを作成する
    - 選択値のバリデーションを実装する
    - 次のステップへの遷移を実装する
    - _要件: 要件2.2, 要件2.3, 要件2.4_

  - [ ] 5.6 コミットタイムとプロファイリングUIを実装する
    - Daily_Commit_Time選択UIを作成する（4択＋自由記入）
    - 7つのプロファイリング質問UIを作成する
    - 各質問に4択＋自由記入オプションを実装する
    - profile.createツールを呼び出す処理を実装する
    - _要件: 要件3.1, 要件3.2, 要件3.3, 要件3.4〜3.12_

  - [ ] 5.7 マイルストン表示と調整UIを実装する
    - milestone.generateツールを呼び出す処理を実装する
    - 10合目の山のビジュアル表示を実装する
    - 各合目の詳細表示UIを作成する
    - マイルストン修正機能を実装する
    - 承認ボタンとデータ保存処理を実装する
    - _要件: 要件4.4, 要件4.5, 要件4.6_

  - [ ] 5.8 完了画面を実装する
    - 完了メッセージUIを作成する（「山登りの準備が整いました」）
    - 次のステップ（日次クエスト生成）への導線を実装する
    - _要件: 要件5.3, 要件7.5_

- [ ] 6. エラーハンドリングとリカバリー機能の実装
  - [ ] 6.1 クライアント側エラーハンドリングを実装する
    - ネットワークエラー時のWidget State保存を実装する
    - 接続回復後の自動再送信を実装する
    - タイムアウト処理（30秒）を実装する
    - ユーザーフレンドリーなエラーメッセージを実装する
    - _要件: 要件5.5_

  - [ ] 6.2 サーバー側エラーハンドリングを実装する
    - 認証エラー（401）のハンドリングを実装する
    - 認可エラー（403）のハンドリングを実装する
    - バリデーションエラー（400）のハンドリングを実装する
    - サーバーエラー（500）のロギングとレスポンスを実装する
    - _要件: 要件5.4_

  - [ ] 6.3 セッション復元機能を実装する
    - Widget Stateからの状態復元を実装する
    - 中断したステップからの再開機能を実装する
    - 前のステップに戻る機能を実装する
    - _要件: 要件7.3, 要件7.4_

- [ ] 7. セキュリティ機能の実装
  - [ ] 7.1 入力検証とサニタイゼーションを実装する
    - クライアント側の入力検証を実装する
    - サーバー側の入力検証を実装する
    - SQLインジェクション対策（パラメータ化クエリ）を実装する
    - XSS対策（入力のサニタイズ）を実装する
    - プロンプトインジェクション対策を実装する
    - _要件: 要件6_

  - [ ] 7.2 監査ログを実装する
    - API呼び出しのロギングを実装する
    - ユーザー同意/拒否のロギングを実装する
    - データアクセスのロギングを実装する
    - _要件: 要件6.4_

- [ ] 8. 統合とE2Eテストの実装
  - [ ] 8.1 オンボーディングフロー全体のE2Eテストを作成する
    - プライバシー同意からマイルストン承認までのフローをテストする
    - エラーケースのテストを作成する
    - セッション復元のテストを作成する
    - _要件: 全要件_

  - [ ] 8.2 5分以内完了のパフォーマンステストを実施する
    - 各ステップの所要時間を測定する
    - ボトルネックを特定し最適化する
    - _要件: 要件1.8_

- [ ] 9. ドキュメントとデプロイ準備
  - [ ] 9.1 プライバシーポリシーを作成する
    - データ収集内容を文書化する
    - 利用目的を明記する
    - 保存期間と削除方法を記載する
    - _要件: 要件6.2, 要件6.5_

  - [ ] 9.2 API ドキュメントを作成する
    - 各エンドポイントの仕様を文書化する
    - MCPツールの使用方法を文書化する
    - _要件: 全要件_

  - [ ] 9.3 デプロイ設定を作成する
    - 環境変数の設定を文書化する
    - データベースマイグレーションスクリプトを作成する
    - デプロイ手順を文書化する
    - _要件: 全要件_
